summary: Update Course
tasks:
  - id: postgres_course_update
    fn: com.gs.datastore
    args:
      datasource: postgres
      data:
        where:
          id: <% parseInt(inputs.params.id) %>
        data: <% inputs.body %>
      config:
        method: Course.update
